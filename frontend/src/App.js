import React from 'react';
import {BrowserRouter as Router, Switch, Route} from 'react-router-dom'

const App = props => {
  return (
    <Router>
      <h1>hi</h1>
    </Router>
  );
};

export default App;
